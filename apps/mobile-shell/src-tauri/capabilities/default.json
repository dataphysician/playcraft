{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local Playcraft shell capability set. The builder runs in the bundled webview or optional loopback HTTP service and does not need filesystem, shell, or microphone permissions for text/transcript input records.",
  "windows": ["main"],
  "permissions": []
}
